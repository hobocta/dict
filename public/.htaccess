<IfModule mod_rewrite.c>
    RewriteEngine On

    # Убедитесь, что запросы обрабатываются через index.php
    RewriteCond %{REQUEST_FILENAME} !-f
    RewriteCond %{REQUEST_FILENAME} !-d
    RewriteRule ^(.*)$ index.php [QSA,L]
</IfModule>
